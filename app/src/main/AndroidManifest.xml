<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lllbmaps">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_txt"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LLLBMaps">
        <activity
            android:name="com.example.AppMoviment.PerfilActivity"
            android:exported="false" />
        <activity
            android:name="com.example.AppMoviment.ui.login.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login" />
        <activity
            android:name="com.example.AppMoviment.MonitorActivity"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAPMcmGwFDEA1S0H3UZI8xjycr9Ku8c0kQ" />

        <activity
            android:name="com.example.AppMoviment.MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps"
            tools:ignore="Instantiatable" />
        <activity
            android:name="com.example.AppMoviment.CreditosActivity"
            android:exported="true" />
        <activity
            android:name="com.example.AppMoviment.ConfiguracoesActivity"
            android:exported="true" />
        <activity
            android:name="com.example.AppMoviment.HistoricoActivity"
            android:exported="true" />
        <activity
            android:name="com.example.AppMoviment.MonitorActivity"
            android:exported="true" />
        <activity
            android:name="com.example.AppMoviment.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.AppMoviment.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>